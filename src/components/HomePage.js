import React from 'react';
import AuthContainer from '../containers/AuthContainer';

const HomePage = () => {
  return (
    <div>
      <AuthContainer />
    </div>
  );
};

export default HomePage;
